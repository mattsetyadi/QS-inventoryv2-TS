import React from 'react';

const InventoryComponent = (props: any) => {
  console.log('inventory component props', props);
  return (
    <div>
      <h1>Inventory Page</h1>
    </div>
  );
};

export default InventoryComponent;
